---
version: 1
slug: "src-app-data-page-tsx"
primary_target: "src/app/data/page.tsx"
related_targets: ["src/app/data/entries.ts","src/app/data/entry-shell.tsx","src/app/data/entry-placeholder.tsx","src/app/data/protected-entry-gate.tsx","src/app/data/data-room-gate.tsx","src/app/data/meta/page.tsx","src/app/data/orbio/page.tsx","src/app/data/hype/page.tsx","src/app/dash/open-compute-inference/page.tsx","src/app/dash/hobbyist-inference-economics/page.tsx"]
---

# Baloch Digital Data Room

- Scope and mode: the public `/data` directory, shared entry gates, and five new placeholder pages; visitor mode is Operate, with selecting and opening a resource as the primary task.
- Audience and job: visitors looking for Baloch Digital research need to scan the available theses and dashboards, understand access status, and reach their selected resource.
- Content: Dashboards appears first, listing public `Open Compute Inference` at `/dash/open-compute-inference` and protected `Hobbyist Inference Economics` at `/dash/hobbyist-inference-economics`. Theses follows with `$NOCK` at `/data/nock`, `$META` at `/data/meta`, `$ORBIO` at `/data/orbio`, and `$HYPE` at `/data/hype`.
- Action and access: directory rows show Public, Locked, or Unlocked. Each of the five protected routes presents a page-local password gate when needed; the existing shared password unlocks all five for the session. Locking ends that shared access. The directory has no password explanatory footer note and retains the authenticated Lock protected pages action. The directory returns Home, entry gates and placeholders return to Data Room, and the unlocked NOCK header offers Data Room and Lock.
- Direction: inherit the existing bone, graphite, sage, Space Mono, grid, and orbit language. Desktop places the two-line identity without a subtitle left and directory right; mobile stacks them. Larger, bold entry titles establish the directory's scanning hierarchy. Gates and placeholders place their own resource title above a compact ruled body. The shared Get in touch rail stays at the viewport bottom throughout, with content clearance at page endings.
- Constraints: extend navigation and access without changing the existing NOCK report content or the global visual system. Keep access labels explicit, preserve keyboard focus and mobile usability, and retain reduced-motion navigation behavior.
- Future content: the three new theses and two dashboards currently show concise Coming soon states. Their substantive content remains to be supplied; do not invent research, data, or investment claims.
